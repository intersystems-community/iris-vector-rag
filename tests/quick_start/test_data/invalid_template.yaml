# Invalid template for testing validation disabled scenarios
metadata:
  version: 1.0  # Should be string, not number
  # Missing schema_version

database:
  iris:
    host: localhost
    port: "invalid_port"  # Should be integer
    namespace: USER
    # Missing username and password

storage: null  # Should be object

vector_index:
  dimension: "invalid_dimension"  # Should be integer
  metric: "invalid_metric"  # Should be enum value

embeddings:
  provider: "invalid_provider"  # Should be enum value
  # Missing model and dimension

llm:
  provider: "invalid_provider"  # Should be enum value
  temperature: 3.0  # Should be <= 2
  max_tokens: -100  # Should be >= 1
  # Missing model

performance:
  batch_size: 0  # Should be >= 1
  max_workers: 100  # Should be <= 32
  timeout: -5  # Should be >= 1

sample_data:
  document_count: 20000  # Should be <= 10000
  source: "invalid_source"  # Should be enum value

mcp_server:
  port: 80  # Should be >= 1024
  enabled_tools: ["invalid_tool"]  # Should be "tools", not "enabled_tools"